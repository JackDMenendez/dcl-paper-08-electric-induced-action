numpy
scipy
sympy
matplotlib
pytest

# dcl_core engine pin (v0.3.0: Peierls gauge coupling + calibration boundary).
# Used only to cross-check the symbolic electric-block derivation against the
# engine's magnetic Wilson-loop holonomy (the {4,4,16} anchor, exp_04). The
# derivation itself is pure sympy; this pin is the consistency-check dependency.
dcl_core @ git+https://github.com/JackDMenendez/dcl-core@v0.3.0
